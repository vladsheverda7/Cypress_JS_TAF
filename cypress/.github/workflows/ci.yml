name: Cypress Tests

on:
  push:
    branches:
      - 'feature/*'

jobs:
  cypress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run Cypress
        run: npx cypress run
